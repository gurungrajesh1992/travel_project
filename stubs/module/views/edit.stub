<x-admin-layout title="Edit {{ title }}">
    <x-slot name="header">Edit {{ title }}</x-slot>

    <x-ui.card>
        <form method="POST" action="{{ route('admin.{{ routeName }}.update', ${{ variable }}) }}">
            @csrf
            @method('PUT')
            @include('admin.{{ viewDir }}.form')
        </form>
    </x-ui.card>
</x-admin-layout>
