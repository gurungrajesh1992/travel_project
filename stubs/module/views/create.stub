<x-admin-layout title="Add {{ title }}">
    <x-slot name="header">Add {{ title }}</x-slot>

    <x-ui.card>
        <form method="POST" action="{{ route('admin.{{ routeName }}.store') }}">
            @csrf
            @include('admin.{{ viewDir }}.form')
        </form>
    </x-ui.card>
</x-admin-layout>
